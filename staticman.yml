collections:
  blog:
    name: "Blog Posts"
    path: "src/content/blog/{options.slug}.md"
    format: "markdown"
    generatedFields:
      pubDate:
        type: "date"
        options:
          format: "iso8601"
    fields:
      - title
      - description
      - author
      - tags
      - category
